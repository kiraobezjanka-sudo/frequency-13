{
  "name": "frequency-13",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/game-core.test.mjs",
    "check": "node --check game-core.js && node --check game.js && node --check scripts/build.mjs",
    "build": "node scripts/build.mjs"
  }
}
